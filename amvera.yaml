---
meta:
  environment: python
  toolchain:
    name: pip
    version: "3.12"

build:
  requirementsPath: requirements-amvera.txt

run:
  scriptName: run_bot.py
  # Data монтируется прямо в /app/data — без симлинков с /data
  persistenceMount: /app/data
